# 交接 Prompt：T13 → T16

> 用法：把「第二部分」整段复制进新工具的对话框，并按「第一部分」把资料附上。
> 这份文件本身不是项目代码，用完可以删。

---

## 第一部分 · 要给对方什么资料

### 必给（4 份，缺一不可）

| 资料 | 路径 | 为什么必须给 |
|---|---|---|
| 收敛版需求说明 | `收敛版需求说明.md` | §3.1 档案 8 项、§3.2 意向分级、§3.3 十四项指标阈值、§3.5 话术标准，全部口径的唯一来源。**不给这份，对方会自己编行业和字段，这是上一轮踩过的坑。** |
| 开发任务清单 | `AI销售复盘助手_开发任务清单_v2.md` | T13/T14/T15/T16 的原文验收标准 |
| 逐字稿类型定义 | `frontend/src/types/types.ts` | T13/T14 的格式必须逐字符符合 `TranscriptSegment` |
| 评分阈值配置 | `frontend/src/config/scoring.ts` | 样本 A 要「预期 0~1 分」、样本 B 要「预期 3~4 分」，必须照着阈值反推 |

### 建议给（能大幅提升产出质量）

| 资料 | 路径 | 作用 |
|---|---|---|
| 现有种子脚本 | `backend/src/seed.ts` | 里面已有 6 段装修逐字稿 + 完整 metrics/scores 摆法，是现成的风格与结构范本 |
| 表结构 | `backend/src/db/schema.ts` | T15 要往库里灌数据时需要 |
| 技术方案 | `技术方案_专业版.md` | 背景补充，非必需 |

### 不用给

`node_modules/`、`backend/data/app.db`（二进制）、`frontend/` 下的页面代码（这几个任务不碰前端）。

---

## 第二部分 · 直接复制这段给对方

我在做一个「AI 销售复盘助手」的 MVP Demo。技术栈：后端 Node + Hono + Drizzle + SQLite(better-sqlite3)，前端 React + Vite + Tailwind。项目根目录已是 git 仓库。

### 当前进度

T01~T12 已完成并验证通过：

- `backend/src/db/schema.ts` —— 8 张表（customers / intent_logs / visits / reviews / needs / products / todos / scripts）。JSON 字段用 `text + { mode:'json' } + $type<T>()`，Drizzle 自动序列化；时间字段用 `integer + { mode:'timestamp' }`，运行时是 JS Date；布尔用 `integer + { mode:'boolean' }`。
- `backend/src/routes/crud.ts` —— 通用 `GET/POST/DELETE /api/:table`，纯表级读写，无业务语义。
- `backend/src/seed.ts` —— 种子脚本，`npm run db:seed` 执行，幂等。已灌入：customers 7、intent_logs 2、visits 8、reviews 6、needs 21、products 10、todos 10、scripts 9。
- `frontend/src/types/types.ts` —— 全局类型（Transcript / Metrics / Scores / AiResult）。
- `frontend/src/config/scoring.ts` —— 14 项指标阈值与维度得分规则。

行业口径：**产品库覆盖教培/装修/广告三行业；客户与逐字稿统一落在装修**（一家叫「合筑装饰」的整装公司，销售是家装顾问，客户是业主）。

### 本次要做的任务

按顺序做 **T13 → T14 → T15 → T16**，任务原文见《开发任务清单》，验收标准以清单原文为准。

**T13 逐字稿样本 A（打得差的）**
手写一份约 30 分钟的拜访逐字稿，必须具备：销售说话明显过多、客户提出至少 2 个异议、结尾没锁定下一步、有一处产品参数说错。预期得分 0~1 分。

**T14 逐字稿样本 B（打得好的）**
与 A 形成明显对照：销售说话占比低、开放式提问多、异议都接住、结尾锁定了下一步。预期得分 3~4 分。

**T15 种子数据：客户与历史复盘**
- S1 客户：档案 8 项**只填 3 项**，无复盘记录
- S2 客户：档案填 6 项，有 1 条复盘
- S3 客户：档案填满，有 2 条复盘，且两条里**同一个维度都是 0 分**（演示「连续在同一处摔跤」）

**T16 种子数据一键重置**
一条命令：清空数据库 → 重新灌全部种子数据。命令要写进 README。

### 硬性约束

1. **口径只能来自《收敛版需求说明》，不许自己编。** 尤其注意这几条容易搞错的：
   - 行业只能是教培 / 装修 / 广告（§2.1）
   - 意向分级 **A = 已成单**，不是「高意向」；`intent_score` 仅 B 类取 1~3，A/C/D 一律记 0（§3.2）
   - 关注维度只能是 **价格 / 质量 / 服务 / 周期** 四类的排序（§3.1 第 6 项）
   - 采购角色只能是 产品使用者 / 成单影响者 / 拍板人（§3.1 第 3 项）
   - 话术五段式阶段名：开场破冰 / 需求确认 / 方案呈现 / 异议处理 / 下一步锁定（§3.5）
   - 需求层级 L1 显性 / L2 潜在 / L3 无需求（§1 功能 #5）

2. **逐字稿格式逐字符对齐 `types.ts` 的 `TranscriptSegment`**：`{ speaker: 'sales' | 'customer', start: number, end: number, text: string }`，`start`/`end` 单位是**秒**（浮点），按 start 升序。约 30 分钟意味着最后一句的 end 在 1700~1900 之间。

3. **`Metrics` 是 16 个字段 = 14 项指标**，别数错：D2 的 `open_question_count` / `total_question_count` 合起来是「开放式提问占比」一项，D3 的 `need_matched_count` / `need_total_count` 合起来是「需求-卖点对齐率」一项。`scores` 的 `d1~d4` 只能是 0 或 1，`total` 必须严格等于 `d1+d2+d3+d4`。

4. **不许改** `schema.ts`、`crud.ts`、`index.ts`、`dify.ts`。

5. **seed 里不许写业务计算逻辑**。S1/S2/S3 由 reviews 条数派生、意向分级由信号判定，都是前端计算层（T17~T24）的事。种子脚本只负责把数据摆成能被算出正确结果的样子。

6. 外键必须用 `.returning().all()` 拿到的真实自增 id 串联，不许硬编码 1/2/3。清空时先删子表再删父表：needs → todos → scripts → reviews → intent_logs → visits → customers → products。

7. 逐字稿要写成像样的销售对话，不要 `test1`/`aaa` 这种占位符——前端页面要拿这批数据做视觉验收。

### 需要你注意的两个已知冲突

**冲突一：T15 与现有种子数据的档案完整度不一致。**
现有 `seed.ts` 里 7 个客户的档案 8 项基本都填满了，而 T15 要求 S1 只填 3 项、S2 填 6 项。请把现有客户的档案按 T15 要求**改稀疏**，而不是新增一批客户——否则会出现两套语义打架的 S1 客户。改的时候注意：留空的字段要挑「优先级低」的留空（§3.1 的缺口优先级列），这样作战包「必拿回的 3 件信息」才算得出合理结果。

**冲突二：T15 要求 S3 客户两条复盘同一维度都是 0 分。**
现有两个 S3 客户里，何薇的两条复盘（d1d2d3d4 = 0000 和 1110）已经满足——两次 D4 都是 0。张国庆的两条是 1100 和 1111，不满足。**建议保留张国庆「从 2 分改进到 4 分并当场签约」这条主线**（这是演示时最有说服力的对照），让何薇去承担「连续在同一处摔跤」的验收点即可。请先确认这个理解，再动手。

### 另外

`backend/src/seed.ts` 里有一处**有意为之**的边界，注释已写明，请勿「修掉」：客户郑帆唯一未满足的需求是价格异议，在 `products.selling_points` 里没有对应卖点，因此他的「应讲未讲」清单为空，会触发《需求说明》§3.4 的「清单为空 → 回退 S1 规则」分支。另一个 S2 客户刘敏的清单非空，两条分支各覆盖一个。

### 工作方式

- 每做完一个任务，报告改了哪些文件，并附验证命令与实际输出。
- 不要替我 git commit，除非我明确要求。
- 如果发现我给的任务描述与《收敛版需求说明》冲突，**先停下来告诉我，不要自己选一个**。
