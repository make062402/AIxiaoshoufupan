# AI 销售复盘助手 · 技术方案（讲解版）

> 这份是给非技术同学看的。内容和专业版完全一致，只是换了说法。
> 遇到不认识的词，翻到最后有对照表。

---

## 开头：我们到底在造什么

一个网页——手机上能装成 App，电脑上直接开网页，两边功能一模一样。

销售用它干三件事：

- 出门见客户前，看一份"这个人是谁、今天该聊什么、讲哪两个产品"的准备材料
- 回来后把这次谈话的记录传上去，看系统给的复盘
- 平时管管自己的客户、话术、产品资料

技术方案要回答的只有一句话：**这个网页背后，得有哪些东西撑着。**

### 关于"手机 App + 电脑网页"是怎么做的

**只写一套代码，让它自己适应屏幕。**

同一份页面代码，手机上打开显示手机版排布（底部四个标签页），电脑上打开显示电脑版排布（左侧导航、内容分栏）。然后把这套网页用一个叫 Capacitor 的工具"套个壳"，就变成能装到手机上的 App——有图标、能全屏，看着和普通 App 没区别。

**为什么不写两套？** 因为销售在电脑上也要干全套活儿，两边功能完全一样。如果写成两套代码，那 14 个指标的算法、四个维度的打分逻辑就得写两遍，你调一次门槛要改两个地方，迟早不一致。写一套就没这个问题。

代价是手机 App 用不了一些深层的系统功能（比如在后台一直录音）。但你们本来就不做 App 内录音，所以不影响。

**两边的排布差别：**

| | 长什么样 | 哪边更合适 |
|---|---|---|
| 手机 / App | 底部四个标签页 | 作战包——出门前 60 秒扫一眼，就该在手机上 |
| 电脑 / 网页 | 左侧导航，内容可以分栏 | 复盘报告——左边看指标右边看谈话记录，点一下就能定位到原话，比手机上好用太多；上传录音可以直接拖进来；产品库录十几个产品的参数，也是电脑上顺手 |

有三条要注意：
- 电脑版不是把手机版横向拉宽，导航要从底部挪到左侧
- 作战包那四块是按手机节奏设计的，电脑上要限制宽度、两边留白，别铺满
- 主管看的报告（表格和趋势图）主要给电脑端用，手机上看不清

---

## 一、用了哪些工具，为什么选它们

### 整体思路

**能简单就简单。**这是给答辩和演示用的版本，不是要扛几千人同时在线。所以凡是"以后可能需要、现在用不上"的东西，一律不做。

最重要的一个决定是：**把所有算账的活儿放到手机端去做，服务器那边只负责记账。**这样服务器的代码能压到很少，而且几乎不用改。

### 手机上跑的部分

| 用什么 | 它是干嘛的 | 为什么选它 |
|---|---|---|
| **React** | 把数据变成页面的工具 | 用的人最多，让 AI 帮你写代码时出错最少。你后面要靠 AI 一点点加功能，这比"技术上哪个更先进"重要得多 |
| **Vite** | 开发时用的工具 | 你改一行代码，浏览器立刻就变了，不用等 |
| **Tailwind** | 调样式的方式 | 样式直接写在页面标签里，不用另开文件。AI 写界面基本不出错 |
| **TypeScript** | 给数据加"标签" | 你的数据挺复杂——一份谈话记录、14 个指标、四个维度的分。不加标签的话，字段名打错了要等到页面跑起来才发现。**这是唯一一个"现在多花点功夫、以后一定省事"的选择** |
| **Capacitor** | 把网页打包成手机 App 的工具 | 不用改一行业务代码，套个壳就能装到手机上。大概半天工夫 |

有个叫 **Next.js** 的东西没用，因为它会把手机端和服务器端的代码混在一起写。方便是方便，但你现在恰恰需要**能把服务器那部分单独讲清楚**，分开更好。

### 服务器上跑的部分

| 用什么 | 它是干嘛的 | 为什么选它 |
|---|---|---|
| **Node.js** | 让服务器能跑代码 | 和手机端用同一种语言，你只学一套 |
| **Hono** | 收发数据的框架 | 极简。一个接口三行代码，全部写在一个文件里也不会乱 |
| **SQLite** | 数据库 | 它就是一个文件。不用安装、不用启动、不用配密码。备份就是复制这个文件，搞砸了删掉重来 |
| **Drizzle** | 定义"表长什么样"的工具 | 你用代码写清楚每张表有哪些字段，跑一条命令自动建好。**这个文件本身就是你答辩最好的材料**，比截个数据库界面的图有说服力 |

没用 **Python**：不是不好，是你手机端已经用 JavaScript 了，再学一套 Python 的环境配置，花的时间不如省下来。

没用 **MySQL**：那种数据库要单独装、单独开、配账号密码。你 demo 的数据量，SQLite 完全够。而且以后真要换，改动很小。

### 外面挂着的服务

| 用什么 | 它干什么 |
|---|---|
| **Dify** | 所有需要"读懂人话"的活儿都交给它。只开两个：一个管复盘分析，一个管生成话术。开多了以后难管 |
| **腾讯云的语音转文字** | 把录音变成文字。**demo 阶段先不接**，见下面说明 |

### 关于录音转文字，为什么先不做

接真的转文字服务，意味着要多做一整套东西：排队机制、进度查询、失败了怎么重来、程序崩了怎么恢复。这是整个方案里**最费工夫的一块**。

而它产出的东西（录音变成文字），演示的时候根本没人会盯着看——评委关心的是复盘报告长什么样、准备材料够不够狠。

所以：**上传录音这个动作做真的**（文件真存下来、数据库真记一笔），但从录音到文字这一步，用提前准备好的文字稿顶上。

这不是偷工减料——你们功能清单第 15 条原文就写了"支持直接粘贴已转好的文字记录"。演示时如实说"转文字用腾讯云，接口验过能用，demo 为了省时间用的准备好的样本"，说出来没人扣分，藏着才有风险。

将来要补上，大概再花一天。

### 放在哪里跑

用你现有的那台阿里云服务器（2 核 2G，够用），配上：
- **PM2**：程序万一挂了自动重启
- **Nginx**：负责把访问请求转给你的程序，顺便配好安全证书
- 域名用 woshimake.com 的一个子域名

---

## 二、这套东西长什么样

### 四层结构

```
 第一层  页面
        销售看到、点到的四个标签页
        它只负责好看好点，不负责思考
          ↓
 第二层  算账的
        数数：说了多少话、打断几次、寒暄多久
        数完对照门槛，四个维度各给 0 分或 1 分
        这一层跑在销售的手机里，不联网
          ↓
 第三层  管账本的伙计（约 150 行代码）
        很笨，只会两件事：存进本子、从本子里翻出来
        它不知道什么叫"四维度"，也不知道什么叫"作战包"
          ↓
 第四层  账本
        一个文件，八张表
        记着所有客户、复盘、产品、待办

 旁边挂着  AI（Dify）
        读谈话记录，说出客户想要什么、
        哪句话说得好、有哪些该讲的没讲
```

### 最关键的一件事：14 个指标，谁来算

四个维度加起来一共 **14 个指标**。它们分两种：

**第一种，代码自己能数（10 个）**——就是纯粹的计时、数数、算比例：

| 指标 | 怎么数 |
|---|---|
| 寒暄花了多久 | 进入正题前用了几秒 |
| 销售打断了几次 | 客户还没说完销售就开口的次数 |
| 客户第一次主动开口 | 第一句不是回答的话，出现在第几秒 |
| 销售说话占多少 | 两边说话时长一比 |
| 客户问了几个问题 | 数问号 |
| 讲了几个卖点 | 对照产品库，命中几条 |
| 客户反复追问同一件事 | 数次数 |
| 异议接住了几成 | 接住的 ÷ 总共提的 |
| 接住异议要多久 | 从客户提出到销售正面回应的间隔 |
| 有没有约好下一步 | 时间、动作、责任人齐不齐 |

**第二种，得让 AI 来数（4 个）**——因为要"听懂意思"才能数：

| 指标 | 为什么代码数不了 | AI 给什么 |
|---|---|---|
| 开放式提问占多少 | "是不是""对不对"能靠关键词认出来，但"您这边现在什么情况"这种没法穷举 | 一共问了几个、其中几个是开放式 |
| 8 项信息问出来几项 | 客户说"预算二十来万"——这算不算问出了预算？得理解意思 | 8 项里问出了哪几项 |
| 需求和卖点对上几成 | 得先知道客户提了什么需求，再看销售有没有接住 | 每条需求有没有被回应 |
| 参数讲错几处 | 得跟产品库比对，还要判断"含糊带过"算不算错 | 讲错或含糊的条数 |

**这里有一条铁律：AI 只负责数出个数，绝不给分。**

打分永远是代码干的——代码拿到"20 个提问里 12 个是开放式"，自己算出 60%，对照 50% 的门槛，判定达标。

这样做的好处：同一份记录跑两次，数字可能有一点点差别，但不会出现"今天 3 分明天 2 分"那种飘。而且每一分都能翻回记录第几分几秒，指着某句话说"就是这里"。

**之前砍掉的那两个指标**（客户互动度、方案支撑度）问题就在这儿——它们没有可数的东西，只能让 AI 直接下结论，那分数就飘了。

### 完整跑一遍

**销售传记录，出复盘：**

```
销售粘贴一份谈话记录
   │
   ├─→ 手机里那层自己数出 10 个指标
   │
   └─→ 同时把记录发给服务器，服务器转给 AI
         AI 返回：另外 4 个指标的计数
                 + 客户提了哪些需求（带原话和时间点）
                 + 哪句话说得好、哪里该改
                 + 这次答应了客户什么
                 + 哪些卖点该讲没讲
                 + 下次见面优先做的三件事
   ↓
14 个指标凑齐 → 对照门槛 → 四个维度各 0 或 1 分 → 总分
   ↓
"代码算的分" + "AI 写的评语" 拼成报告
   ↓
存进账本，同时把需求、待办也记下来
   ↓
页面显示复盘报告，每个分数点开能看到是哪句话
```

**下次要见同一个客户：**

```
销售点开作战包
   ↓
服务器一次性给齐：这人的档案、上次复盘、所有产品
   ↓
手机里当场拼出四块：
   ① 客户信息 8 项，没填的标"待确认"
   ② 这次必须问出来的 3 件事（从空着的字段里按重要性挑）
   ③ 谈判五段式话术（AI 生成，或用准备好的通用话术）
   ④ 推荐 2 个产品
      · 第一次见：按最低价推
      · 见过面了：按上次没讲到的卖点推，
        客户明说过的需求优先于推断出来的
   ↓
显示
```

**注意这里**：作战包**不是提前做好存着的，是每次打开现拼的**。所以它永远是最新的，不会出现"复盘明明更新了、作战包还是旧的"这种事。

### 三个设计上的决定，答辩时值得主动讲

**一、算账的活儿放手机端，不放服务器。**
因为这些计算不需要查数据库也不需要联网。放手机端的好处是，你调门槛的时候改个数字、刷新页面就看到新分数了，不用重启任何东西。答辩时还能当场改一个门槛给评委看分数怎么变。

**这条能成立，前提是手机和电脑用的是同一套代码。**只有一份算账的代码，才不会出现两边算得不一样。哪天真要改成"手机上是另写的原生 App"，这个决定就得推翻——算账的活儿必须挪回服务器，不然要写两遍。

**二、服务器只管存取，不管业务。**
它 150 行代码的意思是：它压根不知道什么是四维度，只知道"把这条记录存进复盘表"。所以它几乎不用改，你后面所有迭代都在手机端那边。

**三、AI 通过服务器转发，不从手机端直连。**
如果手机端直接连 AI 服务，打开浏览器就能看到你的密钥，别人可以拿去刷你的账。**这也是"为什么必须要有服务器"的一个实在理由**——答辩被问到可以直接这么答。

---

## 三、账本里都记些什么

一共八张表。

```
── 客户表 ──────────────────────────────
称呼、身份、联系方式
采购角色（用的人 / 有影响的人 / 拍板的人）
预算区间
核心需求与购买意向
关注排序（价格 / 质量 / 服务 / 周期）
注意事项（沟通偏好、最怕什么出问题、以前不愉快的经历）
采购时间点 / 交付期限
行业
意向分级（A 已成单 / B 中意向 / C 低意向 / D 无意向）
意向强度分（0-3，用来区分 B 和 C）
这个意向是不是人工改过的

※ 上面这些对应需求文档里的"档案 8 项"
※ 客户是 S1 还是 S3，不记在这儿——它是算出来的

── 意向改动记录表 ──────────────────────
谁、在什么时候、把 C 改成了 B
（需求里要求"留痕"，就是这张表）

── 日程表 ──────────────────────────────
什么时候见谁、见完了没、是第几次见

── 复盘表 ──────────────────────────────
谈话记录原文
14 个指标数出来的值
四个维度的分和总分
AI 写的那部分（亮点、改进、承诺、漏讲错讲）

── 需求表 ──────────────────────────────
这次客户提了什么需求
是明说的（L1）还是推出来的（L2）
客户原话是什么、在第几秒说的
这条需求有没有被销售接住

── 产品表 ──────────────────────────────
名字、价格、关键参数
卖点（★这个最关键，见下面）
常见异议怎么答

── 待办表 / 话术库表 ───────────────────
比较简单，略
```

### 有一个字段特别重要：卖点

**卖点不能存成一段话。**必须拆开写清楚三样东西：

```
这条卖点讲的是什么     →  "收纳能力"
话术原文是什么         →  "这套柜体做了三段式分区，
                          同样面积多出 40% 的收纳"
客户说什么的时候用它   →  ["空间小", "东西多", "放不下", "收纳"]
```

**最后那行是灵魂。**没有它，客户说"我家东西多放不下"，程序压根不知道产品库里哪条卖点是接这句的。那"该讲没讲"这个功能、还有"需求和卖点对上几成"这个指标，就全成空话了。

**你造假数据的时候，这块最需要用心。**别的可以随便编，这个得认真编。

### 谈话记录长什么样

```
第 0 到 12.5 秒     销售说：王总您好...
第 12.5 到 20.1 秒  客户说：嗯，你说
```

两件事要注意：
- 时间统一用**秒**，不要一会儿秒一会儿毫秒
- 这个格式要**照着腾讯云真实返回的样子来定**。虽然现在用的是准备好的样本，但格式对上了，将来接真服务时算指标的代码一行都不用改。现在图省事随便定，将来一定返工

### "8"这个数字要小心

它同时管着两件事：

- 客户档案有几项字段 → 8 项
- 判断"关键信息覆盖率"时的分母 → 也是 8

现在一样，但不见得永远一样。哪天你们给档案加一项变成 9 项：
- 如果分母还写死是 8，评分就**悄悄算错了**，还不报错
- 如果分母自动跟着变成 9，那达标线从"问出 4 项"变成"问出 4.5 项"——到底向上还是向下取整？没人定过

**所以这两个数要分开写，还要在旁边注明它俩的关系。**这是全套代码里唯一一个"一个数字管两件事"的地方。

### 还有三个设计决定

**客户是 S1 还是 S3，不存在账本里。**
因为它是算出来的：复盘过 0 次是 S1，1 次是 S2，2 次以上是 S3。存成字段的话，早晚出现"明明复盘两次了，状态还写着 S1"。**能算出来的东西就别存。**

**作战包不建表。**
需求文档写的"复盘结果回写下次作战包"，说的是业务上发生了什么，不是代码该怎么写。实际做法是每次打开现拼，这样数据永远新鲜。

**意向分级要有"人工改过"这个标记。**
不然下次复盘一跑，系统自动判定又把销售手动改的结果冲掉了，用户会觉得这系统跟他对着干。有了这个标记，自动判定只记录建议值，不直接改。

---

## 四、以后自己改代码，最容易栽跟头的 8 个地方

按栽的概率从高到低排。

### 1. 门槛数字写在了好几个地方

"五项满足四项""销售说话不超过 60%""8 项里问出 4 项"——这些数字很容易一边写在算分的代码里，一边写在页面文字里。

你改了一处忘了另一处，页面上写着"满足 3 项达标"，实际按 4 项算，**而且完全不报错**，你根本发现不了。

**怎么办**：所有门槛只写在一个文件里，别处都从这里取。你们组后面肯定还要调这些数，这是长期风险。

### 2. 时间单位一会儿秒一会儿毫秒

"寒暄花了 90 秒"变成"90000 秒"，判定全错。

最难受的是它**不报错**，就是那个维度分数一直给 0。你会以为是判定逻辑写错了，查半天查不出来。

**怎么办**：数据一进来就统一成秒，函数名里带上"秒"字提醒自己。

### 3. 分不清谁是销售

现在用你自己标好的记录没问题。但将来接了真的转文字服务，它只会说"说话人 1""说话人 2"，不知道谁是销售。

而"销售说话占比""销售打断几次"全靠这个。想当然按"先开口的是销售"会经常错——很多拜访是客户先说话。

**怎么办**：现在就在上传页留一个"谁是销售"的选择项，哪怕暂时用不上。

### 4. 把算出来的东西存进账本

除了 S1/S2/S3，还有"还有哪些需求没被满足""这客户复盘过几次"。这些一存成字段，迟早跟真实情况对不上。

**判断标准很简单**：这个值能从别的数据推出来吗？能，就别存。

### 5. AI 返回的东西时好时坏

大模型偶尔会多包一层符号、少个字段、或者干脆回你一段道歉的话。

代码要是直接假设它一定规规矩矩，一遇到意外就崩，**页面直接白屏**。

**怎么办**：拿到结果先清理格式 → 逐项检查有没有缺 → 缺的给个默认值 → 实在不行重试一次。

**演示现场翻车，九成死在这儿。**

### 6. 改了数据格式，老数据读不出来

谈话记录、卖点这些是打包存进去的。你后面加个字段、改个名字，之前存的就对不上了，一读就崩。

**怎么办**：读的时候永远给个兜底值。demo 阶段数据少，实在不行清空重新灌一遍也行。

### 7. 改了表结构，忘了跑同步命令

你在代码里改了字段，但没跑那条"同步到数据库"的命令，代码和账本对不上，报的错还看不出是这个原因。

**怎么办**：把这条命令写在项目说明的第一行。改完立刻跑，再启动。

### 8. 密钥传到 GitHub 上去了

AI 服务的密钥放在一个专门的文件里，这个文件**必须**加进"不要上传"的清单。

新建项目第一件事就做这个，不然等你传上去再删，密钥已经留在历史记录里了，删不掉。

---

## 五、名词对照表

### 打比方的说法 ↔ 真实名字

| 前面怎么说的 | 真实叫什么 | 这个项目里具体用的 |
|---|---|---|
| 页面 / 前台 | 前端、客户端 | React + Vite + Tailwind + TypeScript |
| 算账的 | 前端计算层 | metrics.ts、scoring.ts、battlecard.ts |
| 管账本的伙计 | 后端、服务端 | Node.js + Hono |
| 账本 | 数据库 | SQLite |
| 定义表长什么样的工具 | ORM | Drizzle |
| 一张表 | table | customers、reviews… |
| 谈话记录 | 逐字稿 | transcript |
| AI | 大模型工作流 | Dify |
| 录音转文字 | 语音识别 / ASR | 腾讯云 ASR（demo 阶段不接） |
| 分辨谁在说话 | 说话人分离 | 同上 |
| 前台和伙计之间递条子 | HTTP 接口 / API | `/api/reviews` 这类 |
| 服务器 | 云主机 | 阿里云 ECS |
| 程序挂了自动重启 | 进程守护 | PM2 |
| 把请求转给程序 | 反向代理 | Nginx |
| 安全证书 | HTTPS 证书 | Certbot（免费） |
| 密钥 | API Key | 存在服务器的环境变量里 |
| 打包存进去的数据 | JSON 字段 | transcript、selling_points 等 |
| 假数据 | 种子数据 | 3 个客户 + 2 份记录 + 10 个产品 |
| 一套代码适应两种屏幕 | 响应式布局 | 用 Tailwind 的断点写 |
| 把网页套壳成 App | 打包 / 混合应用 | Capacitor |
| 手机上另写一套的 App | 原生应用 | React Native / Flutter（本项目不采用） |

### 需求文档里的术语

| 术语 | 意思 |
|---|---|
| S1 / S2 / S3 | 这个客户见过几次：0 次复盘是 S1，1 次是 S2，2 次以上是 S3 |
| A / B / C / D | 意向分级：已成单 / 中意向 / 低意向 / 无意向 |
| L1 / L2 / L3 | 需求的三种：客户明说的 / 从话里推出来的 / 没有需求 |
| D1 ~ D4 | 四个评价维度：开场信任 / 需求挖掘 / 价值传递 / 异议处理 |
| 作战包 | 出门前 60 分钟推给销售的准备材料，四块内容 |
| 逐字稿 | 录音转成的文字，标好了谁在第几秒说了什么 |
| 参考线 | "正常水平是多少"，用来判断实测值是高是低 |
| 连接件 | 把拜访前、拜访后串起来的数据传递点 |

---

## 六、最后一句话

**数数的活儿代码干，写字的活儿 AI 干。**

分是代码数出来的，所以销售质疑"凭什么给我这个分"，能翻回记录第 8 分 32 秒指着那句话说"就是这里"。客户想要什么、话该怎么改，才交给 AI。

这一条是你们这个产品跟市面上那些"给通用话术建议"的 AI 工具最大的区别。**方案怎么简化都行，这条不能动。**

---

## 七、后面怎么一天天往前推

### 每个任务走一遍这五步

**第一步：开一段新对话，把该给的都给它**

每次开工，先给 AI 三样东西：
- 任务清单里那一条的原文，包括"怎么算做完了"
- 技术方案里相关的那一节
- 前面做了什么（如果这个任务是接着上一个的）

**别指望它记得上次的事。**每段对话都是从零开始，你不给背景，它就自己猜，猜错了你还看不出来。

**第二步：让它写，你先别看代码**

看代码对你收益不大。直接跑起来，看能不能用。

**第三步：你亲自验一遍**

按验收标准点一遍。**这一步不能交给 AI**——它会倾向于告诉你"已完成、符合预期"。而验收标准写的是"打开某页面应该看到什么"，这只有你自己点得出来。写成这个形式，本来就是为了让 AI 没法自己给自己盖章。

**第四步：没过，把现象说清楚**

要具体："我点了提交，页面白屏了，控制台报了这段错"——把报错原文整段贴给它。比说"不好使"有用一百倍。

**第五步：过了就提交，写上任务编号**

---

### 一个关键习惯：一段对话只干一个任务

做完一个就开新对话，别在一段里连着做三四个。

因为对话越长，AI 越容易被前面的内容带偏，也越容易顺手改坏之前已经跑通的东西。开新对话相当于给它一张干净的桌子。

配套的就是提交 git：每个任务提交一次，万一它把之前的改崩了，你能退回到上一个能跑的版本。

**这是你最重要的安全网。**单人做项目，"能退回到昨天那个能跑的版本"是唯一靠得住的补救办法。

---

### 卡住了怎么办

**同一个问题让它改三次还没好，就停下来。**

这时候多半是方向错了，越改越乱，还会积攒一堆看不懂的代码。

正确做法是退回到方案层面：想清楚要什么、它给了什么、差在哪，再回去让它重写。方案层面的疑问（"这个功能到底该怎么设计"）直接找人聊，别在代码里硬试。

---

### 以后会遇到的几种改动

**改门槛数字**——最常见。跑了真实录音发现分数拉不开差距，回去调。

只改一个配置文件，刷新页面就生效，不用重新部署。**这就是当初把算账的活儿放在手机端的好处。**

**给客户档案加字段**——比如从 8 项加到 9 项。

记得那个"8 管着两件事"的坑：档案字段数和覆盖率分母要一起看，还要定清楚 4.5 项怎么取整。

**改 AI 的提示词**——不动代码，在 Dify 后台改。但要留意额度，见下面。

**加页面**——前端加就行，服务器那边基本不用动。

**加数据表**——改完表结构定义，**必须立刻跑一次同步命令**，不然代码和数据库对不上，报的错还看不出原因。

---

### 什么时候算"大改"，得重新商量

这四件事超出日常迭代，遇到了别自己硬上：

| 什么情况 | 什么时候会遇到 | 大概要多久 |
|---|---|---|
| 接真的录音转文字 | 要给真实用户用、或要处理真实长录音 | 一天 |
| 换数据库 | 多个人同时用，SQLite 顶不住了 | 半天 |
| 算账的活儿挪回服务器 | 哪天改成"手机上另写一套原生 App" | 两天 |
| 主管端接真实数据 | 主管要看真实提交的数据，不是预置的假数据 | 要重新设计 |

---

### Dify 额度要省着用

免费额度只有 200 次，而且是一次性的，不是每月给。开发时调一次提示词就烧一次，很容易见底。

**省额度的办法：**

1. **写页面的时候用假返回值**——你手写一段 JSON 当成 AI 的结果，把页面渲染、异常兜底全调通。这个阶段一次真实调用都不用花
2. **只在两个时候调真的**：调提示词效果（T18）、整体验证（T22）
3. **T22 做完查一次剩多少**，给演示彩排留够
4. **万一用完了**：换个账号，或者改成直接调免费的大模型（智谱那个 GLM-4.7-Flash 是长期免费的），提示词从 Dify 搬到代码里，半天工夫

---

### 提交 git 的几条约定

- 一个任务提交一次，说明写成 `T13 完成：D1 两项指标`
- **第一次推送之前，先把 `.gitignore` 配好**，把密钥文件、依赖包文件夹、数据库文件写进去
- GitHub 仓库建**私有**的
- 每做完一个阶段推一次到 GitHub

**提交不是为了给别人看，是为了给自己留后路。**
