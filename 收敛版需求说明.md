# AI 销售复盘助手 · 收敛版需求说明

> 口径来源优先级：**策略组文档 > V2.7 需求文档 > 标注版功能清单**
> 本文只做收敛，不新增设计。文中 ⚠️ 标记处为优先级规则无法裁决的遗留项，见第五章。

---

## 一、核心功能点

共 5 模块 24 项，全部 P0。

### M0 基础底座（产品库 + 客户数据）

| # | 功能 | 收敛后口径 |
|---|---|---|
| 1 | 产品库导入与管理 | 参数 / 卖点话术 / 常见异议答法。测试行业：教培、装修、广告 |
| 2 | 客户档案 8 项字段管理 | 详见 3.1。关注维度为**四类**（价格 / 质量 / 服务 / 周期） |
| 3 | 客户状态自动判定 | S1 首次接触（已完成复盘 = 0）、S2 二次跟进（= 1）、S3 多次跟进（≥ 2） |
| 4 | 意向分级判定 + 手动覆盖留痕 | **A 已成单 / B 中意向 / C 低意向 / D 无意向**，判定信号见 3.2 |
| 5 | 需求三级识别标记 | L1 显性需求（客户原话说出口的）、L2 潜在需求（由原话推断的真实诉求）、**L3 无需求（无具体场景需求）** |
| 6 | 四维度过程指标计算标准 | 共 14 项客观指标，采用策略组指标集与门槛。代码自算 10 项、AI 计数 4 项，判定一律由代码做，详见 3.3 |
| 7 | 参考线管理 | 取自行业公开研究或厂商官网，每条须带来源名 + 年份 + 样本量 + 原始口径。只用于判断高低，不参与 0/1 判定。本期按教培 / 装修 / 广告配置，其余走默认值 |

### M1 日程、待办与拜访前准备（流程线 A）

| # | 功能 | 收敛后口径 |
|---|---|---|
| 8 | 拜访日程创建与客户绑定 | 客户进入系统的主入口。新客户手动建档 / 老客户手动选择，创建后生成待办并触发作战包排程 |
| 9 | 待办自动生成与截止日管理 | 复盘产出的补救动作与下次优先事项按截止日生成待办，回写到该客户下次日程 |
| 10 | 日程列表与管理 | 创建、查看、编辑；为作战包定时推送提供触发时点 |
| 11 | 作战包推送 | 日程开始前 60 分钟推送 |
| 12 | 作战包四块内容 | ①客户详细信息（含忌讳风险提示）②本次目标与必拿回的 3 件信息 ③谈判五段式框架 + 话术 ④相关产品推荐 **2 个**。挑选规则见 3.4，话术合格标准见 3.5 |

### M2 拜访后上传与转写（流程线 B）

| # | 功能 | 收敛后口径 |
|---|---|---|
| 13 | 上传时新客户手动建档 | **P0**（功能清单该行优先级为空，按 V2.7 正文补齐） |
| 14 | 补充信息表单 | 客户信息或从客户库选择、场景标签（一次 / 二次 / 多次拜访）、录音来源与语言、行业 |
| 15 | 语音转写 + 说话人分离 | 腾讯云 ASR，产出带角色标记与时间戳的逐字稿。支持直接粘贴已转好的文字记录跳过转写 |

### M3 复盘后分析（流程线 C）

| # | 功能 | 收敛后口径 |
|---|---|---|
| 16 | 复盘概览 | 六块：四维度评分（**本次总分 0–4 分 + 该销售历史场次总分平均**）／ 客观指标 + 原话回溯定位（时间戳 + 原对话）／ 亮点与改进点（可一键存话术库）／ 价值传递准确性检测（漏讲错讲）／ 本次承诺清单 ／ 下一步待办 |
| 17 | 下次接触重点 | 补救动作（可直接发微信的话术）+ 下次见面优先做的三件事 |
| 18 | 客户档案确认回写 | 冲突字段提示确认后再覆盖，不静默改写 |
| 19 | 复盘结果一键回写下次作战包 | 写入三样：下次三件事、未被满足需求对应的必讲卖点、自动生成的待办 |
| 20 | 主管端团队 / 个人复盘报告 | 只读报告视图，不做组织架构与权限配置。**入口挂在「我的」二级页，按角色显示**，窄屏底部四 Tab / 宽屏左侧导航均不新增一级入口。宽屏为该功能的主要承载形态 |

### M4 我的

| # | 功能 | 收敛后口径 |
|---|---|---|
| 21 | 客户库 | 按字母顺序排序，显示意向分级标签 + **意向强度分 `intent_score`（0–3）**，用于区分 B 与 C（定义见 3.2） |
| 22 | 优秀话术库 | **个人级**。冷启动提供对标五段式的通用话术；日常从复盘亮点一键存入 |
| 23 | 产品库 | 销售侧查阅入口，与 #1 同一份数据，需含细节参数 |
| 24 | 配置后端 API Key | Dify 工作流接口 Key，MVP Demo 阶段用 |

---

## 二、目标用户与最核心场景

### 2.1 目标用户

| 角色 | 本期定位 | 核心诉求 |
|---|---|---|
| **一线销售** | 主用户，所有交互围绕他设计 | 出门前知道该聊什么；回来不用自己回忆写报告 |
| **销售主管** | 次要，只给只读报告视图 | 不陪访也能看到过程质量，知道该带谁、带哪一块 |
| 企业 / 组织 | 本期不作为独立角色 | 经验沉淀为组织资产（本期为副产品） |

行业范围：教培、装修、广告，各导入一套产品库做验证。

### 2.2 最核心使用场景

**同一个客户的第二次拜访。**

> 第一次见完 → 上传录音 → 出复盘 → 结论（下次三件事、没讲到的卖点、待办）自动写进该客户的下一次作战包 → 第二次出门前 60 分钟收到推送，照着打。

第一次拜访时系统只有销售手填信息，能给的东西有限，与通用话术工具拉不开差距；**产品价值从第二次开始显现**——上一次的结论成为下一次的起点。

验收上以此为唯一 P0 主线：同一客户完成「建档 → 作战包 → 上传复盘 → 回写下次作战包」完整两轮，第二次作战包能带出第一次的复盘结论。

### 2.3 产品形态

**手机端 App + 电脑网页端，两端功能等同。**销售在电脑上可完成全套操作（建档、看作战包、上传复盘、查看报告），不做功能阉割。

实现方式为**响应式单一代码库**：同一套页面代码，按屏幕宽度切换布局；手机端在此基础上打包为可安装的 App。两端不是两套产品，只是同一套界面的两种排布。

| | 布局 | 差异 |
|---|---|---|
| **窄屏（手机）** | 底部四 Tab：待办 / 新建 / 复盘 / 我的 | 作战包为主要使用场景（出门前快速扫读） |
| **宽屏（电脑）** | 左侧导航，内容区分栏 | 复盘报告可左右分栏（左指标、右逐字稿），原话回溯定位体验更好；上传素材支持拖拽；产品库批量维护更顺手 |

宽屏布局的三条约束：

- 底部 Tab 在宽屏改为左侧导航，不是把手机布局横向拉伸
- 作战包四块内容为窄屏节奏设计，宽屏时须限制内容区最大宽度并两侧留白，不铺满
- 主管端报告（#20）在宽屏下为主要承载形态，表格与趋势图在手机上可读性差

---

## 三、关键规则明细

### 3.1 客户档案 8 项字段

| # | 字段 | 说明 | 缺口优先级 |
|---|---|---|---|
| 1 | 称呼与身份 | 怎么称呼、在客户单位内的身份 | 高 |
| 2 | 联系方式 | 电话、微信等可触达渠道 | 高 |
| 3 | 在采购中的角色 | 产品使用者 / 成单影响者 / 拍板人 | 高 |
| 4 | 预算区间 | 本次采购能出多少钱 | 高 |
| 5 | 核心需求与购买意向 | 想解决什么问题、买的意愿多强 | 高 |
| 6 | 关注维度优先级排序 | **价格 / 质量 / 服务 / 周期** 四类排序 | 中 |
| 7 | 注意事项 | 含沟通偏好（节奏、场合、忌讳）、风险敏感点（最怕服务出问题还是产品出问题）、过往不愉快经历 | 中 |
| 8 | 采购时间点 / 交付期限 | 什么时候要用上、什么时候必须交付 | 高 |

作战包②「本次必拿回的 3 件信息」：从当前为空的字段中按上表优先级从高到低取前三；同优先级按字段序号在前的优先。

### 3.2 意向分级判定信号

自动判定，销售可手动覆盖并留痕（改前值、改后值、操作人、时间）。同时命中多级时取最高级。

| 级 | 判定信号 |
|---|---|
| **A 已成单** | 有成交金额 / 已签合同 / 口头承诺 + 明确时间点（客户明确说"今天/这周内打款"且给出具体时间） |
| **B 中意向** | 命中以下任一类，按最高权重定级：**决策推进类（3 分）**主动要联系方式 / 报价单 / 合同模板、问流程 ≥1 次；**价格类（2 分）**主动问价格 / 优惠 / 分期 / 付款方式 ≥1 次；**功能细节类（1 分）**主动问参数 / 规格 / 售后 / 质保 / 交付周期 ≥1 次 |
| **C 低意向** | 拖延型（"再看看""考虑一下"且后续无具体时间或金额）／ 比价型（说要对比其他家但未给出对比点）／ 沉默型（销售提问后停顿 >10 秒未回应，且同场出现 ≥2 次，单次不判定）／ 预算时机未定型（"等领导批预算""过阵子再找你"） |
| **D 无意向** | 直接拒绝（拒绝语后 2 轮对话内无任何追问、无主动延续话题）／ 委婉拒绝（"已有供应商""预算取消"且**完全无未来预期、语气坚决**）／ 拉黑失联 |

**边界规则**：委婉拒绝若带有具体或模糊的未来时间条件（"明年再说""等新负责人到位"），不判 D，改判 C 的预算时机未定型。

**意向强度分**：B 类命中的最高权重值（决策推进类 3 / 价格类 2 / 功能细节类 1）单独存为一个字段，命名 `intent_score`，取值 0–3。A / C / D 类记 0。该字段用于客户库列表排序与 B、C 的区分展示（见功能 #21）。

### 3.3 四维度评分口径（采用策略组标准）

四个维度共 **14 项客观指标**，每维度按 0 / 1 二元判定，总分 0–4 分。

「数据来源」列说明该指标由谁算出：**代码**指从逐字稿直接计时、计数、算比例；**AI 计数**指需要理解语义才能数出来，由 AI 返回计数值。**无论哪一种，阈值判定与 0 / 1 打分一律由代码完成**——AI 只出计数，不出分数。这是「评分可举证」成立的前提。

**D1 开场与信任建立**——2 项均满足 → 1 分

| 指标 | 达标线 | 数据来源 |
|---|---|---|
| 破冰时长 | 15 秒 ~ 2 分钟 | 代码 |
| 销售打断次数 / 小时 | ≤ 3 次 | 代码 |

> 策略组原稿的第 3 项「客户互动度（客户是否愿意透露个人信息）」已砍除：没有可数的对象，只能由模型直接下结论，会使 D1 得分不可举证。

**D2 需求挖掘**——5 项满足 4 项 → 1 分

| 指标 | 达标线 | 数据来源 |
|---|---|---|
| 客户首次主动发言时点 | 3 分钟内 | 代码 |
| 客户说话占比 | 销售占比 ≤ 60% | 代码 |
| 关键信息覆盖率 | ≥ 50%（**8 项档案字段中问出 4 项及以上**） | AI 计数：返回问出了哪几项 |
| 开放式提问占比 | ≥ 50% | AI 计数：返回总提问数与开放式提问数 |
| 客户提问数 | 客户追问次数 ≥ 3 | 代码 |

**D3 价值传递**——4 项满足 3 项 → 1 分

| 指标 | 达标线 | 数据来源 |
|---|---|---|
| 卖点提及数 | ≥ 3 个 | 代码：逐字稿命中产品库卖点标签的条数 |
| 需求 - 卖点对齐率 | ≥ 60% | AI 计数：返回每条 L1/L2 需求有无被对应卖点回应 |
| 参数准确率 | 错误次数 = 0 | AI 计数：返回讲错或含糊带过的参数条数 |
| 客户话题反馈数 | **客户未就同一话题反复追问超过两次**（反复追问视为没讲清楚，属扣分信号，原稿方向写反已修正） | 代码 |

**D4 异议处理与推进**——3 项满足 2 项 → 1 分

| 指标 | 达标线 | 数据来源 |
|---|---|---|
| 异议正面回应率 | ≥ 70% 的异议被实质回应（非沉默 / 回避 / 敷衍） | 代码 |
| 异议平均回应时长 | 提出到实质回应间隔 ≤ 10 秒 | 代码 |
| 下一步锁定率 | 时间 + 动作 + 责任人三要素齐全 | 代码 |

> 策略组原稿的第 4 项「方案支撑度（是否有案例、数据、逻辑支撑）」已砍除，原因同 D1。

**合计 14 项：代码自算 10 项，AI 计数 4 项。**

每一分的给出都必须能回溯到逐字稿原话与时间戳。

### 3.4 作战包产品推荐挑选规则（固定 2 个）

**S1 首次接触**——档案信息不足以支撑个性化匹配时：
1. 先分产品类型 → 2. 确定价格区间 → 3. 取最低价位作为默认推荐
   价格并列时取最小产品 ID。

**S2 / S3 跟进**——来源为上次复盘的「应讲未讲」清单（客户已表达的需求对应哪些卖点之前没讲）：
- 清单不为空 → 清单中产品优先推荐
- 清单为空 → 回退 S1 规则
- 清单产品数 > 2 → **取前 2 个**（原稿"回退 S1"为笔误，已修正）。排序依据：先按需求显性程度，对应 **L1 显性需求的产品优先于 L2 潜在需求**；同级时按该需求在上次复盘中被提及的次数由多到少；再并列则取最小产品 ID。

每个推荐产品须同时给出：关键参数、必讲卖点话术、常见异议与答法。数据来源为产品库（#1）。

### 3.5 话术合格标准

1. **简洁**——单条时长不超过 30 秒
2. **可执行**——能照着直接读
3. **异议类必须带追问**——回应后必须带开放式追问
4. **标明所属五段式阶段**——开场破冰 / 需求确认 / 方案呈现 / 异议处理 / 下一步锁定
5. **绑定使用场景**——说明什么情况下使用
6. **来自真实成交场景**——优先取实际录音中已被验证有效的原话
7. **不含未核实参数**——话术中出现的价格必须与产品库一致

### 3.6 复盘机制前置判断

- **有过复盘**：谈判前确认 8 项信息是否完整、是否有未接住的顾虑
- **无复盘**：新客户无二次复盘信息，走 S1 路径

---

## 四、这一版先不做

### 4.1 V2.7 已明确排除

- App 内录音（拜访在线下发生，只做上传）
- 与外部 CRM 双向同步
- **团队公共话术库、跨人共性打法自动入库**（本期话术库为个人级）
- 多语种转写全量覆盖（只做普通话 + 中英混杂）
- 完整组织架构与权限配置

### 4.2 按优先级规则裁定为本期不做

| 项 | 出处 | 裁定理由 |
|---|---|---|
| 优秀销售话术案例集（同一打法 ≥3 次升级为共性案例、同步给团队） | 用户流程第一版 C.2 | 属于「跨人共性打法自动入库」，被 V2.7 明确排除；且需真后端支持多人数据共享，MVP 单机方案跑不了 |
| 团队整体复盘报告分析六块（C.1） | 用户流程第一版 | 同上，跨人归纳结论。主管端本期只保留个人复盘报告只读视图 |
| 参考线两阶段切换（30 场 / 100 场切企业自有线） | 用户流程第一版 0.8 | V2.7 未列入，且本期样本量触发不了 |
| 冷启动闸门 5 / 20 条 | 用户流程第一版 | 依附于团队分析与案例集，后者不做则失去意义 |
| 信息可信度三态标记（🟢已核验 / ⚪手填 / 🟡推断） | 用户流程第一版 0.2 | V2.7 未列入，会给每个字段增加一层状态管理成本 |
| 电话录音自动建档建日程 | 用户流程第一版 A 触发口 3 | 24 项功能清单无对应编号，视为已砍 |
| 连接件②承接件（拜访结束 30 分钟推送上传提醒、预填客户与场景） | 用户流程第一版 | 策略组与 V2.7 均未列入，本期不做 |
| 需求 L3 作为「必讲卖点来源」 | 用户流程第一版 0.5 | L3 采用 V2.7 定义 = 无需求，必讲卖点统一由 **L1 / L2 未被满足的需求**推导 |
| 真实语音转写（demo 阶段） | — | 上传音频入口照做、文件真实落库，但转写走功能 #15 已定义的「粘贴已转好的文字记录」路径，使用预置逐字稿。腾讯云 ASR 接口需验证可用并在演示中如实说明。正式版本补齐 |

### 4.3 保留的连接件

本期只做两个：

- **连接件①　客户信息入口**：待办页手动新建建档并绑定日程（#8）／ 上传录音时新客户手动建档（#13），两条路径写入同一份客户档案
- **连接件③　复盘回写作战包**：复盘完成后写入下次三件事、未满足需求对应必讲卖点、自动待办（#19）

---

## 五、遗留项处理结果

### 5.1 客户库「区分 B 与 C 的指标字段」（功能 #21）—— 已定

原始诉求确认为区分 **B 中意向 / C 低意向**（非 A 与 B）。复用 3.2 的 B 类权重分，新增字段 `intent_score`（0–3），客户库列表显示并参与排序。C 类记 0，天然沉在 B 类之下。

### 5.2 S2/S3 清单产品数 > 2 时的排序依据 —— 已定

L1 显性需求对应的产品优先于 L2 → 同级按该需求在上次复盘中被提及次数由多到少 → 再并列取最小产品 ID。见 3.4。

理由：作战包的作用是先接住客户已经说出口的需求，L1 未被满足比 L2 未被满足更容易在下次拜访中翻车。

### 5.3 主管端入口位置（功能 #20）—— 已定

挂「我的」二级页，按角色显示，不新增一级入口（窄屏四 Tab、宽屏左侧导航均不变）。宽屏为该功能的主要承载形态——表格与趋势图在手机上可读性差。

理由：主管为本期次要角色且只有只读报告，不值得占一级导航；放进「我的」也避免销售端出现一个空 Tab。

### 5.4 复盘概览「平均分」含义 —— 已定

指该销售**历史场次的总分平均**（0–4），非四维度平均。展示形式：「本次 3/4 · 你的历史平均 2.4」。

理由：四维度平均在 0–4 制下只有 5 个取值，无区分度；历史平均才能体现「同一销售随使用时长的过程质量变化」这一结果指标。

### 5.5 性能指标（6.2 六项）—— ⚠️ 唯一未决项

音频时长上限、文件大小上限、转写耗时、复盘产出总耗时、转写准确率仍为待定，须先向腾讯云 ASR 确认单文件硬限制才能填定值。**需由需求组指定确认人，并在框架级对齐 Checkpoint① 之前回填**——这几个数直接决定上传页要不要做分片、要不要做长音频截断提示。

---

## 六、后续调优提示（非本期决策）

- **D1 与 D2 门槛偏严**：D1 要求 2 项全中，D2 要求 5 项中 4 项。真实录音跑起来大概率集体低分。低分本身不是问题（说明确实卡在动作上），但若 demo 阶段需要评分有区分度，D1 可考虑放宽为 2 选 1。建议积累一批样本后再校准，本期不动。
- **「8」这个数字管着两件事**：客户档案有几项字段，与 D2「关键信息覆盖率」的分母是几。二者当前一致，但不必然同步——若将来档案加到 9 项，分母若硬写为 8 则评分悄悄出错；若自动跟随字段数，达标线又从 4 项变为 4.5 项，向上还是向下取整未定。实现时须把「档案字段清单」「覆盖率分母」「覆盖率达标线」分成三个独立配置项，并注明关系。
- **意向分级手动覆盖的留痕数据**：覆盖记录（改前值 / 改后值 / 操作人 / 时间）应回流给策略组用于校准自动判定规则。本期只做记录，不做规则自动调整。
