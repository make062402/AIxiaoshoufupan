# AI 销售复盘助手 · 技术方案（专业版）

> 配套文档：《AI 销售复盘助手 · 收敛版需求说明》
> 适用阶段：MVP Demo。目标为最快可交付、可演示、可答辩，不追求扩展性与高可用。

---

## 一、技术选型说明

### 1.1 整体形态

单体应用，前后端分离部署在同一台云服务器。不引入微服务、消息队列、缓存中间件与容器编排——demo 阶段日均任务量在两位数，这些组件只增加运维负担，不产生收益。

**交付形态：手机端 App + 电脑网页端，两端功能等同。**实现方式为响应式单一代码库——同一套 React 代码按屏幕宽度切换布局，手机端在此基础上用 Capacitor 打包为可安装应用。两端共用同一份代码、同一个后端、同一个数据库，不存在双端实现。

核心取舍：**业务计算全部前置到前端，后端退化为纯数据存取层。**

这么做有四个理由：

- **迭代速度**——指标计算与评分判定是本项目改动最频繁的部分（策略组后续仍会调整阈值与指标构成）。放在前端，改完存盘刷新页面即可看到结果，无需重启服务、无需重新部署。放在后端则每次调阈值都要走一遍发布流程。
- **后端趋于零改动**——后端不感知任何业务语义，只执行表级读写。整个迭代周期内它几乎不需要动，维护面收敛到前端一侧，对单人开发的项目意义很大。
- **答辩可演示**——阈值改动能在评委面前当场生效，直接展示「分数是怎么算出来的、门槛变了会怎样」，比讲解代码有说服力。
- **计算本身无外部依赖**——这 14 项指标的输入只有逐字稿一份数据，不查库、不联网，是纯函数。既然放哪儿都能算，就放在改起来最快的那一侧。

代价是计算逻辑暴露在浏览器端。该问题在 demo 阶段不构成风险——指标口径本就是要向用户公开举证的内容，并非商业机密。

**多端复用不构成问题**：手机端与电脑端为同一代码库的两种布局，计算层只有一份实现，不存在双端逻辑不一致的风险。若改为真原生 App（React Native / Flutter）与网页两套代码，则该结论不成立，计算层必须下沉至后端——那是另一套架构，工时约为当前方案的 2 倍，本期不采用。详见 2.4 架构决策。

### 1.2 前端

| 技术 | 类型 | 选型理由 |
|---|---|---|
| React | UI 框架 | 生态规模最大，AI 辅助编码时的正确率最高。项目后续由非专业开发者配合 AI 迭代，这一点权重高于框架本身的技术优劣 |
| Vite | 构建工具 | 开发态热更新即时，冷启动快；产物为纯静态文件，部署简单 |
| Tailwind CSS | 样式方案 | 原子化 class 写在标签内，无需维护独立样式文件，AI 生成 UI 时几乎不出错 |
| TypeScript | 类型系统 | 本项目数据结构复杂度较高（逐字稿、14 项指标、四维度评分、结构化卖点），无类型约束时字段名错误只能在运行时暴露。这是唯一一处「增加初期成本但必须投入」的选型 |
| Tailwind 响应式断点 | 布局方案 | 以 `md:` 等断点前缀在同一份 JSX 内表达两端布局差异，无需维护独立的移动端 / 桌面端组件树 |
| Capacitor | App 打包 | 将构建产物封装为 iOS / Android 安装包，提供图标、启动页、全屏与系统文件选择能力。不改动任何业务代码，增量工时约 0.5 人日 |

**不选 Next.js 的理由**：Next.js 将前后端耦合在同一项目内，开发便利但削弱了后端的独立可述性。本项目需要在答辩场合单独讲清后端与数据层的设计，前后端分离更有利。

**不选 React Native / Flutter 的理由**：真原生方案意味着手机端与网页端为两套代码库，14 项指标计算与四维度评分逻辑需实现两遍，阈值调整需同步两处，长期必然产生不一致。规避该问题需将计算层下沉至后端，后端规模由约 150 行增至约 400 行，且丧失「改阈值即时生效」的迭代优势。在演示效果与打包方案无实质差异的前提下，不采用。

**两端布局差异**（同一代码库内实现）：

| | 布局 | 说明 |
|---|---|---|
| 窄屏 / App | 底部四 Tab | 作战包为主场景，为快速扫读设计 |
| 宽屏 / 网页 | 左侧导航，内容区分栏 | 复盘报告左右分栏（左指标、右逐字稿），原话回溯定位体验显著优于窄屏；素材上传支持拖拽；产品库批量维护更顺手；主管端报告以此为主要承载形态 |

约束：宽屏不得由窄屏布局横向拉伸得到；作战包四块内容在宽屏须限制内容区最大宽度并两侧留白。

### 1.3 后端

| 技术 | 类型 | 选型理由 |
|---|---|---|
| Node.js | 运行时 | 与前端同语言，降低学习与上下文切换成本 |
| Hono | Web 框架 | 极简，单个路由三行代码，全量路由可维护在单文件内。相比 Express 更现代、体积更小 |
| SQLite | 数据库 | 嵌入式，无独立进程，零安装零配置。备份即复制单个文件，重置即删除重建，极适合 demo 迭代 |
| Drizzle ORM | ORM | 以 TypeScript 定义 schema，一条命令生成并同步表结构。**schema 文件本身即为可交付的数据模型说明文档**，优于截图数据库管理界面 |

**不选 Python / FastAPI**：技术上可行，但前端已确定为 JS 技术栈，额外引入 Python 的虚拟环境与依赖管理，学习成本高于收益。

**不选 MySQL / PostgreSQL**：需独立安装、启动、配置账号与连接池。demo 数据量下 SQLite 完全胜任；且 Drizzle 编写的查询在迁移至 PostgreSQL 时改动极小，升级路径畅通。

### 1.4 外部服务

| 服务 | 承担职责 | 说明 |
|---|---|---|
| Dify | 所有需语义理解的环节 | 仅开两个工作流：①复盘分析（含 4 项指标的计数）②作战包五段式话术生成。工作流数量刻意压到最少，避免版本管理失控 |
| 腾讯云 ASR | 语音转写 + 说话人分离 | **demo 阶段不接入**。走需求功能 #15 已定义的「粘贴已转好的文字记录」路径，使用预置逐字稿 |

**关于 ASR 的阶段性取舍**：接入真实 ASR 意味着必须实现异步任务链路——任务状态表、后台轮询调度、前端状态查询、失败重试与崩溃恢复。这是完整方案中工时占比最高的一块，而其产出（音频转文字）在演示中并非评价重点。故 demo 阶段以预置逐字稿替代，音频上传与落库照常实现。补齐真实 ASR 预估增量工时约 1 人日。

### 1.5 部署

| 组件 | 用途 |
|---|---|
| 阿里云 ECS（2 核 2G / Ubuntu 22.04） | 应用主机，现有资源 |
| PM2 | Node 进程守护，异常自动重启 |
| Nginx | 反向代理、静态资源托管、HTTPS 终止 |
| Certbot | Let's Encrypt 免费证书签发与续期 |

前端构建产物由 Nginx 直接托管，`/api/*` 反向代理至 Hono 进程。域名使用 woshimake.com 子域名。电脑端直接访问该域名；手机端 App 为同一份构建产物经 Capacitor 打包，运行时请求指向同一后端。

---

## 二、架构描述

### 2.1 分层结构

```
┌────────────────────────────────────────────────────────┐
│ L1  展示层  React                                        │
│     待办 │ 新建 │ 复盘 │ 我的                            │
│     纯展示与交互，不含业务判定逻辑                        │
├────────────────────────────────────────────────────────┤
│ L2  计算层  前端纯函数，无网络依赖                        │
│                                                         │
│     metrics.ts     10 项代码可自算指标                   │
│     scoring.ts     14 项指标 → 套阈值 → D1~D4 各 0/1     │
│                    → total 0-4                          │
│     battlecard.ts  档案缺口排序取前 3；                   │
│                    按 S1/S2/S3 规则挑选 2 个产品          │
│                                                         │
│     所有阈值统一读取 config/scoring.ts                   │
├────────────────────────────────────────────────────────┤
│ L3  接口层  Hono，约 150 行                              │
│                                                         │
│     通用 CRUD                                            │
│       GET  /api/:table         列表 / 详情               │
│       POST /api/:table         新增 / 更新               │
│       DEL  /api/:table/:id     删除                      │
│     专用接口                                             │
│       POST /api/upload         接收音频文件               │
│       POST /api/analyze        转发 Dify（服务端持有密钥）│
│       GET  /api/battlecard/:id 聚合返回作战包所需数据      │
├────────────────────────────────────────────────────────┤
│ L4  持久层  SQLite / Drizzle，8 张表                     │
└────────────────────────────────────────────────────────┘
                          ↓ HTTPS
                  ┌───────────────┐
                  │ Dify 工作流    │
                  │ ①复盘分析      │
                  │ ②作战包话术    │
                  └───────────────┘
```

### 2.2 指标计算的职责边界

14 项客观指标按计算方式分为两类，这是本方案最核心的设计约束。

**代码自算（10 项）**——从逐字稿直接计时、计数、算比例，确定性输出：

| 维度 | 指标 | 计算方式 |
|---|---|---|
| D1 | 破冰时长 | 进入业务话题前的累计秒数 |
| D1 | 销售打断次数 / 小时 | 客户发言区间内销售起话的次数，按时长归一 |
| D2 | 客户首次主动发言时点 | 首个非应答性发言的起始秒 |
| D2 | 销售说话占比 | 销售发言总时长 ÷ 双方发言总时长 |
| D2 | 客户提问数 | 客户发言中问句数量 |
| D3 | 卖点提及数 | 逐字稿命中 products.selling_points 标签的条目数 |
| D3 | 客户话题反馈数 | 同一话题下客户追问次数 |
| D4 | 异议正面回应率 | 被实质回应的异议数 ÷ 全部异议数 |
| D4 | 异议平均回应时长 | 异议提出至销售实质回应的间隔均值 |
| D4 | 下一步锁定率 | 结尾段是否含时间 + 动作 + 责任人三要素 |

**AI 计数、代码判定（4 项）**——需语义理解方能计数：

| 维度 | 指标 | Dify 返回 | 代码处理 |
|---|---|---|---|
| D2 | 开放式提问占比 | 总提问数、开放式提问数 | 相除 → 对照 ≥ 50% |
| D2 | 关键信息覆盖率 | 8 项档案字段中问出了哪几项 | 计数 → 对照 ≥ 50%（即 ≥ 4 项） |
| D3 | 需求 - 卖点对齐率 | 每条 L1/L2 需求有无被对应卖点回应 | 算比例 → 对照 ≥ 60% |
| D3 | 参数准确率 | 讲错或含糊带过的参数条数 | 对照 = 0 |

**硬约束：Dify 只返回计数，不返回分数。** 全部阈值判定与 0/1 打分在 `scoring.ts` 内完成。

该约束保证同一份逐字稿重复分析时，计数值可能有轻微波动，但不会出现分数在相邻档位间跳变；且每一项得分均可回指到 transcript 的时间戳区间，满足需求说明中「评分可举证」的验收项。

被砍除的「客户互动度」「方案支撑度」两项，其问题正在于不存在可计数对象，只能由模型直接给出结论性判断。

### 2.3 核心数据流

**流程 B：拜访后复盘**

```
销售粘贴逐字稿（或上传音频 → 返回预置逐字稿）
   │
   ├─→ metrics.ts 计算 10 项指标
   │
   └─→ POST /api/analyze
         └→ Hono 附加密钥 → 调用 Dify 复盘分析工作流
            返回：4 项指标计数
                  + 需求 L1/L2 标记（含原话与时间戳）
                  + 话术亮点 / 改进点 / 承诺清单
                  + 漏讲错讲检出 / 下次见面三件事
   ↓
scoring.ts 汇总 14 项指标 → 读取 config/scoring.ts 阈值
   → D1~D4 各 0/1 → total 0-4
   ↓
组装报告：代码产出的分数 + Dify 产出的文本
   ↓
POST /api/reviews 落库；同步写入 needs、todos 两表
   ↓
渲染复盘概览，各维度得分可展开定位至 transcript 时间戳
```

**流程 A：拜访前作战包**

```
销售打开某客户作战包
   ↓
GET /api/battlecard/:customerId
   → 聚合返回：customers 记录
                + 最近一次 reviews（含 satisfied=false 的 needs）
                + products 全量
   ↓
battlecard.ts 本地组装四块：
   ① 客户信息 8 项，空值标注「待确认」
   ② 本次必拿回 3 件事 ← 空字段按缺口优先级取前三
   ③ 谈判五段式话术 ← 调 Dify 生成，或回落至预置通用话术
   ④ 产品推荐 2 个
        S1：产品类型 → 价格区间 → 取最低价，并列取最小 ID
        S2/S3：上次未满足需求对应卖点，L1 优先于 L2
               → 提及次数降序 → 并列取最小 ID
   ↓
渲染
```

### 2.4 三条关键架构决策

**决策一：业务计算置于前端。**
指标计算与评分判定为纯函数，输入逐字稿输出数值，无数据库与网络依赖。前置至前端后，调整阈值只需修改配置文件并刷新页面，无需重启服务；答辩场景下可现场演示阈值变更对评分的影响。

该决策成立的前提是**单一代码库同时服务两端**。手机 App 与电脑网页共用同一份计算层实现，不存在双端逻辑分叉。若后续改为真原生 App 与网页两套代码，本决策必须推翻，计算层须下沉至后端统一实现。

**决策二：后端不承载业务语义。**
后端不感知「四维度」「作战包」等业务概念，仅执行表级读写。由此后端在整个迭代周期内近乎零改动，全部功能演进集中在前端，降低维护面。

**决策三：Dify 调用经由后端转发。**
前端直连 Dify 将导致 API Key 暴露于浏览器。经后端转发可将密钥限制在服务端环境变量内。此项亦是「本项目为何需要后端」的实质性技术理由之一。

---

## 三、数据存储设计

### 3.1 表结构

共 8 张表。

```
── customers 客户 ──────────────────────────────────
id
name            称呼
identity        身份
phone           联系方式
role            采购角色：使用者 / 影响者 / 拍板人
budget          预算区间
core_need       核心需求与购买意向
priority_order  关注排序 JSON：["价格","质量","服务","周期"]
notes           注意事项：沟通偏好 / 风险敏感点 / 过往不愉快经历
deadline        采购时间点 / 交付期限
industry        行业
intent_level    意向分级 A / B / C / D
intent_score    意向强度分 0-3，用于区分 B 与 C
intent_manual   是否人工覆盖过，boolean
created_at

※ name~deadline 共 9 列，对应需求说明档案 8 项
  （「称呼与身份」在库内拆为两列，业务上计为一项）
※ 客户状态 S1/S2/S3 不设字段，按该客户 reviews 条数实时派生

── intent_logs 意向覆盖留痕 ────────────────────────
id
customer_id
from_level      改前值
to_level        改后值
operator        操作人
created_at

── visits 拜访日程 ─────────────────────────────────
id
customer_id
scheduled_at    计划时间
status          待拜访 / 已完成
scene           场景标签：一次 / 二次 / 多次拜访

── reviews 复盘 ───────────────────────────────────
id
customer_id
visit_id
transcript      逐字稿 JSON，结构见 3.2
metrics         14 项指标计算值 JSON
scores          评分 JSON：{ d1, d2, d3, d4, total }
ai_result       Dify 返回 JSON：亮点 / 改进 / 承诺 / 漏讲错讲 / 下次三件事
created_at

── needs 需求 ─────────────────────────────────────
id
review_id
customer_id
level           L1 / L2 / L3
text            需求内容
quote           客户原话
timestamp_sec   原话在逐字稿中的起始秒
satisfied       本次是否被对应卖点回应，boolean

── products 产品 ──────────────────────────────────
id
name
price
params          关键参数 JSON
selling_points  结构化卖点 JSON，结构见 3.2
objections      常见异议与答法 JSON
industry

── todos 待办 ─────────────────────────────────────
id
customer_id
review_id
text
due_date
done

── scripts 话术库 ─────────────────────────────────
id
stage           五段式所属阶段
scene           使用时机
text            话术原文
from_review_id  来源复盘 ID，通用话术为空
```

### 3.2 关键 JSON 字段结构

**transcript 逐字稿**

```json
[
  { "speaker": "sales",    "start": 0,    "end": 12.5, "text": "王总您好..." },
  { "speaker": "customer", "start": 12.5, "end": 20.1, "text": "嗯，你说" }
]
```

- 时间单位统一为**秒**（浮点）
- `speaker` 枚举仅 `sales` / `customer`
- **该结构须对齐腾讯云 ASR 的实际返回格式**。demo 阶段虽使用预置样本，格式一致可保证后续接入真实 ASR 时 `metrics.ts` 无需改动

**selling_points 结构化卖点**

```json
[
  {
    "tag": "收纳能力",
    "script": "这套柜体做了三段式分区，同样面积多出 40% 的收纳",
    "match_keywords": ["空间小", "东西多", "放不下", "收纳"]
  }
]
```

卖点**不得存储为非结构化文本**。`match_keywords` 是需求与卖点建立映射的唯一依据，D3「需求-卖点对齐率」与「漏讲错讲检测」均依赖该字段。种子数据构造时此项优先级最高。

### 3.3 阈值配置文件

`config/scoring.ts` 中，以下三项须分别独立定义：

```
PROFILE_FIELDS        客户档案字段名数组（当前 8 项）
COVERAGE_DENOMINATOR  D2 关键信息覆盖率分母（当前 8）
COVERAGE_THRESHOLD    覆盖率达标线 0.5，及取整方向
```

`8` 这一数值同时服务于两个语义不同的用途：档案字段数量，与覆盖率计算分母。二者当前一致但不必然同步——档案扩展至 9 项时，若分母硬编码为 8 则评分静默出错；若自动跟随字段数，则达标线由 4 项变为 4.5 项，取整规则尚未定义。

**这是全套代码中唯一一处「单一数值承担双重语义」的位置。**

### 3.4 三条数据模型设计决策

**客户状态 S1/S2/S3 不落字段。** 该值由 `count(reviews where customer_id = ?)` 派生（0 条 S1、1 条 S2、≥2 条 S3）。落为字段将产生「已复盘两次但状态仍为 S1」的不一致。同理，「未被满足的需求」由 `needs where satisfied = false` 查询得出，不冗余存储。

**作战包不建表。** 需求说明中「复盘结果回写下次作战包」为业务动作描述，实现上采用读取时聚合而非写入时同步。该设计消除了作战包与复盘结果之间的数据同步问题。

**intent_manual 标记为必要字段。** 需求要求意向分级支持人工覆盖并留痕。缺少该标记时，下一次复盘的自动判定将覆盖人工修改结果。有该标记时，自动判定仅记录建议值，不直接写入。

---

## 四、后续迭代的高风险点

按发生概率降序排列。

### 4.1 阈值散落多处导致计算与展示不一致

**风险**：`销售占比 ≤ 60%`、`D2 五项满足四项`、`覆盖率分母 8` 等数值同时出现在计算函数与页面文案中。单侧修改后不产生任何报错，页面显示的达标条件与实际判定逻辑静默分歧。

**对策**：全部阈值仅在 `config/scoring.ts` 定义一次，计算与展示均从该处读取。策略组后续仍会调整这些数值，此项为长期风险。

### 4.2 时间戳单位不一致

**风险**：逐字稿时间字段若在某处按毫秒、某处按秒处理，「破冰时长 90 秒」将被计算为 90000 秒。该错误不触发异常，仅表现为某维度长期得 0 分，排查困难。

**对策**：数据入口处统一转换为秒；函数命名显式携带单位（如 `getBreakIceSeconds`）。

### 4.3 说话人角色识别

**风险**：接入真实 ASR 后，返回结果仅标注「说话人 1 / 2」，不指明何者为销售。而「销售说话占比」「销售打断次数」等指标完全依赖该判定。以「首个发言者为销售」作为默认规则的错误率较高——相当比例的拜访由客户先开口。

**对策**：在上传页预留「指定销售方」的手动选择项，即使 demo 阶段使用预置逐字稿暂不启用。

### 4.4 派生值被冗余存储

**风险**：S1/S2/S3、未满足需求列表、客户已复盘次数等可推导值若落为字段，将逐步与源数据脱节。

**判断准则**：任何可由其他数据推导得出的值，一律不存储。

### 4.5 Dify 返回结构不稳定

**风险**：大模型输出可能包裹 Markdown 代码块标记、缺失字段、或返回非 JSON 的自然语言。直接 `JSON.parse` 将抛出异常，在无后端兜底的前端直接表现为白屏。

**对策**：解析前剥离代码块标记 → 解析后逐字段校验 → 缺失字段填充默认值 → 整体解析失败则重试一次后标记为分析失败。演示环节的故障多源于此。

### 4.6 JSON 字段结构演进导致历史数据不可读

**风险**：`transcript`、`selling_points`、`ai_result` 等字段以 JSON 字符串存储，结构变更后既有记录反序列化失败。

**对策**：读取时统一提供默认值兜底。demo 阶段数据量小，必要时清库并重新灌入种子数据亦为可接受方案。

### 4.7 Drizzle schema 变更未执行同步命令

**风险**：修改 schema 定义后未执行迁移命令，代码模型与实际表结构不一致，报错信息不直接指向根因。

**对策**：将迁移命令置于项目 README 首行，形成「改 schema → 立即迁移 → 再启动」的固定动作。

### 4.8 密钥进入版本控制

**风险**：Dify 与腾讯云密钥写入 `.env` 后若未加入 `.gitignore`，一经提交即永久留存于 Git 历史，事后删除文件无法清除。

**对策**：项目初始化第一步即配置 `.gitignore`。

---

## 五、已知技术债

以下限制在 demo 阶段可接受，需在向真实用户交付前处理：

| 项 | 说明 | 触发升级的信号 |
|---|---|---|
| 无异步任务链路 | 未接入真实 ASR，无任务队列与状态机 | 需处理真实长音频 |
| SQLite 并发写入能力有限 | 单文件锁，不适合多用户同时写 | 多销售同时使用 |
| 单机单进程，无水平扩展 | PM2 单实例 | 用户量上升 |
| 主管端数据为预置样本 | 无真实多用户数据聚合 | 主管需查看真实团队数据 |
| 手机端为打包网页而非真原生 | 无法调用深度系统能力（后台录音、系统级通知） | 需 App 内录音或后台常驻能力 |

升级路径：SQLite → PostgreSQL（Drizzle 查询改动极小）；新增 jobs 表与后台调度器接入 ASR（约 1 人日）。

---

## 六、迭代方式

### 6.1 开发期的迭代循环

以任务清单中的单个任务为最小迭代单元，五步闭环：

1. **建立上下文**——新开一段编码代理会话，输入三项：该任务在任务清单中的原文（含验收标准）、技术方案中对应的选型或表结构章节、前置任务的产出说明。代理会话无跨会话记忆，上下文缺失将导致实现偏离既定设计。
2. **实现**——由代理完成编码，不逐行审阅代码。
3. **验收**——由人工按验收标准实操验证。此步不可委托给代理：验收标准以「打开某页面应看到什么」形式书写，其目的正是提供代理无法自证的外部检验。
4. **反馈**——未通过时提供具体现象与完整报错原文，而非结论性描述。
5. **提交**——通过后执行 git commit，message 以任务编号开头。

**约束：一段会话对应一个任务。** 会话上下文越长，代理越易受先前内容干扰并回改已验收的代码。任务间切换即新开会话。

**熔断规则**：同一问题连续三轮修改仍未通过，停止迭代。此时多为设计层面偏差，继续修补会累积技术债。应退回到方案层面重新分析。

### 6.2 变更类型与处理路径

| 变更类型 | 频度 | 影响面 | 处理方式 |
|---|---|---|---|
| 调整评分阈值 | 高 | 仅 `config/scoring.ts` | 修改配置项，前端刷新即生效，无需重新部署。这是「计算层置于前端」这一架构决策的主要收益 |
| 调整指标构成（增删指标） | 中 | `metrics.ts` + `scoring.ts` + 复盘页展示 | 需同步更新需求说明 3.3 的指标表与「数据来源」标注 |
| 客户档案增减字段 | 中 | schema + 表单 + 覆盖率计算 | 须同时评估 `PROFILE_FIELDS`、`COVERAGE_DENOMINATOR`、`COVERAGE_THRESHOLD` 三项配置及取整规则 |
| 调整 Dify 提示词 | 高 | Dify 工作流，代码无改动 | 注意 Sandbox 额度消耗，见 6.4 |
| 新增页面或表单 | 中 | 前端 + 通用 CRUD 接口 | 后端通常无需改动 |
| 新增数据表 | 低 | schema + 迁移命令 | 改 schema 后必须立即执行迁移，否则代码模型与实际表结构不一致 |

### 6.3 架构级升级触发条件

以下变更超出常规迭代范畴，需重新评估架构：

| 升级项 | 触发信号 | 改动范围 | 预估 |
|---|---|---|---|
| 接入真实 ASR | 需处理真实长音频；或需向真实用户交付 | 新增 jobs 任务表、后台轮询调度器、前端状态查询、失败重试与崩溃恢复 | 约 1 人日 |
| SQLite → PostgreSQL | 多用户并发写入 | Drizzle 查询语句改动极小，主要为部署与连接配置 | 约 0.5 人日 |
| 计算层下沉至后端 | 改为真原生 App 与网页双代码库 | 迁移 metrics / scoring / battlecard 三个模块，后端由约 150 行增至约 400 行，丧失「改阈值即时生效」 | 约 2 人日 |
| 主管端接入真实团队数据 | 主管需查看真实提交数据而非预置样本 | 需引入用户体系与数据归属，涉及权限模型 | 需重新设计 |

### 6.4 Dify 额度约束下的调试策略

Dify 云版 Sandbox 档为一次性 200 次查询额度，且自托管需 2 核 4G 环境（现有 ECS 为 2 核 2G，无法承载）。故调试须节流：

- **前端联调阶段使用固定 mock 返回值**——手写一份符合契约的 JSON 作为 Dify 返回，完成页面渲染、字段兜底、异常分支的全部调试
- **仅在 T18 提示词调优与 T22 整体验证时调用真实接口**
- **T22 完成后核对剩余额度**，为 T43 演示彩排预留余量
- 额度耗尽后的备选：更换账号，或切换至免费模型 API 直连（智谱 GLM-4.7-Flash 等长期免费档），后者需将提示词从 Dify 工作流迁移至代码内，约 0.5 人日

### 6.5 版本控制约定

- 每任务一次 commit，message 格式：`T{编号} 完成：{任务名}`
- `.gitignore` 须在首次 push 前配置完成，至少包含 `.env`、`node_modules`、数据库文件
- 远程仓库设为私有
- 每完成一个阶段（A~F）推送一次远程

**commit 的作用不在协作，在回滚。** 单人开发场景下，「能退回到上一个已验收的版本」是代理误改既有代码时唯一可靠的补救手段。
