# T25-T28 第四阶段交接文档

> 用法：在新对话中，让 Codex 先阅读本文件，然后把下方“可直接复制的提示词”整段发给它。  
> 本轮只做 T25-T28，必须严格串行：完成一个任务、自检、验证、提交 commit 后，才能开始下一个任务。

## 一、当前项目状态

- 当前分支：`main`
- T17-T24 已全部完成并提交，最新提交为：
  - `4c3d2f4 T24 完成：计算层整体验证`
  - `ad3b3dc T23 完成：mock 合并 14 项指标`
  - `75c632f T22 完成：评分判定函数`
  - `d48b370 T21 完成：D4 三项指标`
  - `c903176 T20 完成：D3 两项代码指标`
  - `26ccd6d T19 完成：D2 三项代码指标`
  - `8642bac T18 完成：D1 两项指标`
  - `9ee44d0 T17 完成：逐字稿基础工具函数`
- T24 的固定基准：
  - 样本 A：总分 `1`，四维分数 `1/0/0/0`
  - 样本 B：总分 `3`，四维分数 `0/1/1/1`
  - 运行 `node --experimental-strip-types scripts/verifyCalculationLayer.mjs`，应打印两张 14 项指标表，并通过 `6/6` 项检查。
- T24 验证使用本地 mock，并通过 fetch 陷阱确认没有调用 Dify、没有消耗额度。

## 二、新对话必须先读的文件

先读项目根目录的约束文件（如存在 `AGENTS.md`），再读：

- `docs/进度.md`
- `收敛版需求说明.md`
- `AI销售复盘助手_开发任务清单_v2.md`（定位 T25-T28 原文）
- `技术方案_专业版.md`（重点看接口、Dify 和容错相关章节）
- `backend/mock/README.md`（当前唯一 Dify 输入输出契约）
- `backend/mock/difyResult.json`
- `backend/src/dify.ts`
- `backend/src/index.ts`
- `backend/.env.example`
- `.gitignore`
- `frontend/src/types/types.ts`
- `frontend/src/api/client.ts`
- `frontend/src/lib/reviewAnalysis.ts`
- `frontend/src/samples/transcriptA.ts`
- `frontend/src/samples/metricEvidence.ts`
- `scripts/verifyCalculationLayer.mjs`
- 前后端的 `package.json`

## 三、已有实现，禁止盲目重写

当前代码中已经存在以下基础能力：

- `backend/src/dify.ts`
  - 已有 Markdown 代码围栏剥离。
  - 已有 AI JSON 解析与字段归一化。
  - 已有缺失字段默认值和空结果。
  - 已有失败后重试一次（最多请求两次）的结构。
  - 已有 `USE_MOCK` 开关和本地 mock 加载。
- `backend/src/index.ts`
  - 已有 `POST /api/analyze`。
  - 已有逐字稿参数校验。
- `frontend/src/api/client.ts`
  - 已有通用 `/api` 请求封装，但还没有业务分析页面。
- `frontend/src/lib/reviewAnalysis.ts`
  - 已能合并 10 项代码指标和 4 项 AI 指标。

因此，T25、T26 必须先对照任务验收标准审计现有实现，再补缺口、补自动化验证。不要为了“看起来做了很多”而重构已经正确的逻辑。

## 四、可直接复制到新对话的提示词

```text
请根据项目文档继续完成任务 T25-T28。

先完整阅读：
1. docs/交接_T25-T28.md
2. docs/进度.md
3. 收敛版需求说明.md
4. AI销售复盘助手_开发任务清单_v2.md 中 T25-T28 的原文
5. 交接文档列出的相关代码与契约文件

严格规则：

- 本次只做 T25、T26、T27、T28，绝对不要顺带实现 T29 或其他任务。
- 必须严格按 T25 → T26 → T27 → T28 串行执行。
- 每次只执行一个任务。
- 每个任务必须依次完成：检查现状 → 实现或补缺口 → 自检 → 自动化验证 → 查看 git diff → 更新 docs/进度.md → 提交一个独立 commit。
- 当前任务没有验证通过并提交 commit 前，禁止开始下一个任务。
- 不允许先把四个任务一起改完，再统一验证或统一提交。
- T25、T26 已有部分基础实现，必须先审计，不要盲目重写。
- 不得为了让真实 Dify 结果过关而篡改样本、14 项指标算法、评分阈值、mock 期望值或产品数据。
- 不得把 DIFY_API_KEY 写入前端、源码、文档、日志或 commit；也不要在回复中打印密钥内容。
- 不要提交 backend/.env；确认它被 git 忽略。
- T27、T28 才允许真实调用 Dify。T25、T26 必须通过 mock/fetch stub 完成，真实调用次数为 0。
- 真实调用要节省额度。每完成 T27、T28 都检查并汇报调用次数或剩余额度，但不要泄露密钥。
- 如果 Dify 未登录、工作流无法访问、契约存在必须由我决定的冲突，停止当前任务并明确告诉我，不得假装验证通过。
- 每一步完成后，先自检，再向我汇报：
  1. 改动了哪些文件；
  2. 每处改动用大白话实现了什么；
  3. 自动化验证命令和实际结果；
  4. 我该打开哪个页面、敲哪条命令、点哪个按钮，应该看到什么；
  5. 本任务的 commit hash。

开始前先执行只读检查：git status --short、git log -8 --oneline，并运行 T24 基线命令：
node --experimental-strip-types scripts/verifyCalculationLayer.mjs
确认工作区和基线没有异常后，只开始 T25。

T25 要求：Dify 返回解析与兜底

- 对照任务原文审计 backend/src/dify.ts 已有的代码围栏剥离、字段校验、默认值和重试一次逻辑。
- 只修缺口，并新增可重复执行的自动化验证脚本或测试。
- 至少验证：
  1. Markdown 的 `json` 代码围栏包裹的合法 JSON 能正常解析；
  2. 缺少 highlights 时不抛异常，并得到默认空数组；
  3. 返回中文道歉等非 JSON 文本时不抛异常，最终返回完整默认结构；
  4. 正常 mock 的结果与 backend/mock/difyResult.json 契约一致；
  5. 首次返回异常时会重试一次；连续异常时总请求次数恰好为 2，不会无限重试；
  6. 测试期间真实 Dify 调用数为 0。
- 同时执行后端类型检查和与本次改动相关的回归验证。
- 通过后更新进度并独立提交，建议提交信息：
  T25 完成：Dify 返回解析与兜底
- 提交后才能开始 T26。

T26 要求：后端 /api/analyze 转发接口

- 审计已有 POST /api/analyze，只补验收缺口。
- 用 USE_MOCK=true 做后端端到端验证，建议新增独立验证脚本。
- 至少验证：
  1. 传入合法 transcript 后返回 ok=true、source=mock 和完整 result；
  2. 缺少或传入空 transcript 时返回 400 和可读错误；
  3. 非法 JSON 请求不会让服务崩溃；
  4. 前端侧能够通过 /api/analyze 调用接口；可以补一个类型安全的 API 函数，但不要做 T29 的页面；
  5. frontend 目录及前端构建产物中没有 DIFY_API_KEY、Bearer 密钥或真实 Dify 地址调用；
  6. 密钥只允许存在于被忽略的 backend/.env，本任务真实 Dify 调用数为 0。
- 执行前后端类型检查、前端 lint/build 和相关回归。
- 通过后更新进度并独立提交，建议提交信息：
  T26 完成：后端分析转发接口
- 提交后才能开始 T27。

T27 要求：配置并验证真实 Dify 工作流

- 严格以 backend/mock/README.md 为唯一输入输出契约，并结合 T09 文档执行。
- Dify 输入必须是四个字段：transcript、selling_points、profile_fields（前三项按契约传 JSON 字符串）和 industry（文本）。
- 输出必须只有契约规定的 7 个 AI 字段，不能让 Dify 输出评分。
- open_question_count 必须是数字“次数”，不是得分。
- 产品参数判断必须遵守：只比较销售明确说出口的数字、年份、品牌、范围等参数；没有提到的产品参数不能被误判为含糊或错误。
- 引用原话必须来自逐字稿；高光片段起始句必须能在原逐字稿中精确定位。
- 用样本 A 连续运行 3 次，验证三次都能被后端解析，且字段结构和类型稳定。
- 建议用自动校验脚本检查 7 个字段，而不是只凭肉眼看结果。
- 严格控制真实调用次数，完成后立即检查 Dify 额度/用量并汇报。
- 如果需要在仓库中保存工作流提示词、配置说明或验证脚本，可以提交；不得提交密钥。
- 只有三次真实运行和结构验证都通过后，才更新进度并提交。建议提交信息：
  T27 完成：Dify 复盘分析工作流
- 提交后才能开始 T28。

T28 要求：真实链路联调

- 在本地被忽略的 backend/.env 中临时配置 USE_MOCK=false 和真实 Dify 密钥，绝不读取或输出密钥内容。
- 使用样本 A 和完整产品/画像/行业输入，通过项目自己的 /api/analyze 走真实后端链路，不得绕过后端从前端直连 Dify。
- 将真实 AI 结果接入现有 buildReviewAnalysis，与 10 项代码指标合并成 14 项结果。
- 对照 T24 样本 A 的 mock 基准总分 1，记录真实总分和四维分数；允许总分相差 1。若超出范围，先定位 Dify 提示词或字段问题，不能修改评分算法和样本来硬凑。
- 验证正常链路返回完整结构，并确认前端/调用方拿不到 Dify 密钥。
- 验证断网/请求失败兜底：优先用 fetch stub 抛出网络异常来稳定复现，不要破坏系统网络。应得到可读错误和完整默认 AI 结果，进程不崩溃、不出现白屏式未捕获异常。
- 当前业务页面尚未进入 T29，禁止为了“页面提示”提前搭建完整分析页或报告页。现阶段用接口/集成脚本证明错误可读即可；如果任务原文明确要求一个现有页面上的交互，但仓库确实没有该页面，先向我说明边界并请求确认。
- 真实调用总量控制在任务清单建议的 3-5 次内，完成后立即查看并汇报额度/用量。
- 测试结束后将本地 USE_MOCK 恢复为 true，避免后续误耗额度；.env 仍然不得提交。
- 执行前后端类型检查、lint/build、T24 基线和 T25-T27 的相关回归。
- 通过后更新进度并独立提交，建议提交信息：
  T28 完成：真实链路联调

最终结束前再检查：

- git status --short 没有遗漏的任务代码；
- git log 中 T25、T26、T27、T28 各自有一个独立 commit；
- docs/进度.md 只把实际验证通过的任务标为完成；
- T29 及之后的页面、上传、报告等功能没有被提前实现；
- backend/.env、密钥和真实响应中的敏感信息没有进入 Git。
```

## 五、建议的基础回归命令

新对话应根据实际依赖脚本调整命令，但至少覆盖：

```bash
node --experimental-strip-types scripts/verifyCalculationLayer.mjs

cd backend
npx tsc --noEmit

cd ../frontend
npx tsc --noEmit
npm run lint
npm run build
```

注意：命令应逐条运行并检查退出码，不要通过管道掩盖失败。

## 六、T27/T28 人工检查点

- T27 需要已有可访问的 Dify 工作流和登录状态。如果新对话无法进入 Dify，应停在 T27，不得把仅有本地脚本冒充为真实验证。
- T27 连续三次结果必须符合相同的 7 字段契约，但文案内容可以有合理波动。
- T28 必须从项目的 `/api/analyze` 入口走完整链路；直接调用 Dify API 不能代替该验收。
- T28 的断网验收优先使用可控的网络异常 stub，不必真的关闭整台电脑网络。
- T28 完成后恢复 `USE_MOCK=true`，以保护剩余额度。

## 七、完成后的收尾

T28 验收并提交后：

1. 确认 `docs/进度.md` 已记录 T25-T28 的实际结果和验证命令。
2. 确认四个任务各自对应一个 commit。
3. 本交接文档只是临时文件；在用户确认交接完成后，可另行决定是否删除。不要把删除动作混进 T25-T28 的任何一个任务提交中。
