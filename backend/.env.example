PORT=3000
DB_FILE=./data/app.db

# Dify（服务端持有，禁止写进前端）
DIFY_API_KEY=
DIFY_BASE_URL=https://api.dify.ai/v1

# 配置页管理保护：请使用密码管理器生成高强度随机值；不得放入前端或提交 Git
CONFIG_ADMIN_TOKEN=

# 联调开关：true 时所有 Dify 调用直接返回 mock/difyResult.json，不发网络请求
USE_MOCK=true
