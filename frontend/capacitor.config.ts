import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.aixiaoshou.review',
  appName: 'AI 销售复盘助手',
  webDir: 'dist',
  backgroundColor: '#f8fafc',
  android: {
    backgroundColor: '#f8fafc',
  },
}

export default config
